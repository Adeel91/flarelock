# Shared Backend

This folder is reserved for backend types and utilities that need to be shared by more than one FlareLock service.

The current implementation keeps most code close to the API because there is only one active backend application.

Shared contracts and helpers can move here when additional workers or services become active.
