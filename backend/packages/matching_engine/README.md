# Matching Engine

Private matching currently lives close to the main API so the prototype remains simple and easy to follow.

If FlareLock expands to more markets, order types or independent execution services, the matching logic can move here as a reusable engine.

This folder is reserved for that future separation.
