# Alert Engine

This folder is reserved for execution and lifecycle notifications.

Future alerts could cover matched orders, funding deadlines, settlement failures, Stop Loss triggers, redemption updates and claimable Firelight withdrawals.

Notifications are outside the current prototype scope, so this remains a future module.
