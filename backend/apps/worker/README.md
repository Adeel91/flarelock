# Worker

This folder is reserved for background jobs that should eventually run outside the main FlareLock API.

Likely uses include settlement recovery, scheduled Stop Loss evaluation, redemption tracking and Firelight processing.

The current prototype does not need a separate worker process yet, so this folder is intentionally kept as future structure only.
